build:
  docker:
    web: Dockerfile  # Define o Dockerfile para o serviço principal

run:
  worker:
    command:
      - celery -A core worker --loglevel=info
    image: python:3.12.7-alpine3.20  # Define a imagem Docker para o processo Celery
